---
jobs:
  - name: job-hello-world
    public: true
    plan:
      - task: hello-world
        privileged: true
        config:
          platform: linux
          image_resource:
            type: docker-image
            source: {repository: busybox}
          run:
            path: echo
            args: [hello world]
